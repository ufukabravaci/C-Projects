#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num ;

    printf("Enter the number : ");

    scanf("%d", &num);

    if


        (num %2 == 0)

        // or (num %2 != 0)

            printf  ("%d is an EVEN number.", num);

    else
            printf("%d is an ODD number." ,num);


    return 0;
}
